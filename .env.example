# Supabase Edge Function secret. Set in the NEW project's function settings.
# Exact URLs without trailing slashes; add the final pages.dev address after deployment.
ALLOWED_ORIGINS=http://localhost:4173
# SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY are supplied by Supabase itself.
# Do not copy the service-role key into dist/config.js or GitHub.
